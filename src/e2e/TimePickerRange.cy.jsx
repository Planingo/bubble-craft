import React from 'react'
import { TimePickerRange } from '../components/TimePicker/TimePickerRange'

describe('<TimePickerRange />', () => {
  it('renders', () => {
    cy.mount(<TimePickerRange />)
  })
})