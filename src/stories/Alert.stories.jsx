import { Alert } from "../components/Alert/Alert";

const meta = {
  title: "Atomes/Alert",
  component: Alert,
};

export default meta;

export const Playground = {
  args: {
    message: "Success message !",
    type: "success",
  },
};
