import { Slider } from "../components/Slider/Slider";

const meta = {
  title: "Atomes/Slider",
  component: Slider,
};

export default meta;

export const Playground = {
  args: {
    children: "Slider",
  },
};
