import { Upload } from "../components/Upload/Upload";

const meta = {
  title: "Organismes/Upload",
  component: Upload,
};

export default meta;

export const Playground = {
  args: {
    children: "Upload",
  },
};
