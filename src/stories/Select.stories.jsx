import { Select } from "../components/Select/Select";

const meta = {
  title: "Atomes/Select",
  component: Select,
};

export default meta;

export const Playground = {
  args: {
    children: "Select",
  },
};
