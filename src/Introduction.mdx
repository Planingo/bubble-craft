import {Meta} from '@storybook/blocks';
import {linkTo} from '@storybook/addon-links';
import * as Components from './components';
import { styles, atomes, molecules, organisms, templates, pages } from './generate.json'

export const COMPONENTS_EXCLUDE = ['CardGrid'];

export const getComponentLink = componentName => `Composants/${componentName}`;
export const getComponentLinkAtomes = componentName => `Atomes/${componentName}`;

export const COMPONENTS = ['Card', 'Modal', 'Notif', 'Empty']

<Meta title="Bubble-craft - Introduction" />

<style>
  {`
    .subheading {
      --mediumdark: '#999999';
      font-weight: 900;
      font-size: 13px;
      color: #999;
      letter-spacing: 6px;
      line-height: 24px;
      text-transform: uppercase;
      margin-bottom: 12px;
      margin-top: 40px;
    }

    .link-list {
      display: grid;
      grid-template-columns: 1fr;
      row-gap: 10px;
    }

    .info-status {
      margin: 0.7em;
    }

    #design-system {
      text-align: center;
      margin-bottom: 0.7em;
    }

    #styles, #atomes, #molécules, #organismes, #templates, #pages {
      margin-top: 1.7em;
    }

    @media (min-width: 620px) {
      .link-list {
        row-gap: 20px;
        column-gap: 20px;
        grid-template-columns: repeat(7, 1fr);
      }
    }

    @media all and (-ms-high-contrast:none) {
    .link-list {
        display: -ms-grid;
        -ms-grid-columns: repeat(7, 1fr);
      }
    }

    .link-item {
      padding: 20px 30px 20px 15px;
      border: 1px solid #00000010;
      border-radius: 5px;
      transition: background 150ms ease-out, border 150ms ease-out, transform 150ms ease-out;
      color: #333333;
      display: flex;
      align-items: center;
      cursor: pointer;
    }

    .link-item:hover {
      border-color: #1EA7FD50;
      transform: translate3d(0, -3px, 0);
      box-shadow: rgba(0, 0, 0, 0.08) 0 3px 10px 0;
    }

    .link-item:active {
      border-color: #1EA7FD;
      transform: translate3d(0, 0, 0);
    }

    .link-item strong {
      font-weight: 700;
      display: block;
      margin-bottom: 2px;
    }

    .link-item span {
      font-size: 14px;
      line-height: 20px;
    }

    .link-item span.link-icon {
      margin-right: 10px;
      font-size: 40px;
      width: 40px;
      height: 40px;
      line-height: 40px;
      background-size: auto 20px;
      background-repeat: no-repeat;
      background-position: left center;
    }

    .link-item div.link-icon {
      margin-right: 10px;
      width: 40px;
      min-width: 40px;
      height: 40px;
      line-height: 40px;
      background-size: auto 30px;
      background-repeat: no-repeat;
      background-position: left center;
    }

    .sbdocs {
      max-width: initial;
    }

    #docs-root {
      margin: 2em 4em;
    }

    h1 {
      text-align: center;
    }

    `}
</style>

# Bubble-craft : Design system de Planin'go

## Styles

<div className="link-list">
  {styles.map(style =>
      <a
        key={style.id}
        className="link-item"
        href={`?path=/story/styles-${style.id}--page`}
      >
        <div className="info-status">{style.wip ? '🚧' : '🚀'}</div>
        <span>
          <strong>{style.title}</strong>
          {style.descrition}
        </span>
      </a>
    )}
</div>

## Atomes

<div className="link-list">
  {atomes.map(atome =>
      <a
        key={atome.id}
        className="link-item"
        href={`?path=/story/atomes-${atome.id}--default`}
      >
        <div className="info-status">{atome.wip ? '🚧' : '🚀'}</div>
        <span>
          <strong>{atome.title}</strong>
          {atome.descrition}
        </span>
      </a>
    )}
</div>

## Molécules

<div className="link-list">
  {molecules.map(molecule =>
      <a
        key={molecule.id}
        className="link-item"
        href={`?path=/story/molecules-${molecule.id}--default`}
      >
        <div className="info-status">{molecule.wip ? '🚧' : '🚀'}</div>
        <span>
          <strong>{molecule.title}</strong>
          {molecule.descrition}
        </span>
      </a>
    )}
</div>

## Organismes

<div className="link-list">
  {organisms.map(organism =>
      <a
        key={organism.id}
        className="link-item"
        href={`?path=/story/organisms-${organism.id}--default`}
      >
        <div className="info-status">{organism.wip ? '🚧' : '🚀'}</div>
        <span>
          <strong>{organism.title}</strong>
          {organism.descrition}
        </span>
      </a>
    )}
</div>

## Templates

<div className="link-list">
  {templates.map(template =>
      <a
        key={template.id}
        className="link-item"
        href={`?path=/story/template-${template.id}--default`}
      >
        <div className="info-status">{template.wip ? '🚧' : '🚀'}</div>
        <span>
          <strong>{template.title}</strong>
          {template.descrition}
        </span>
      </a>
    )}
</div>

## Pages

<div className="link-list">
  {pages.map(page =>
      <a
        key={page.id}
        className="link-item"
        href={`?path=/story/pages-${page.id}--default`}
      >
        <div className="info-status">{page.wip ? '🚧' : '🚀'}</div>
        <span>
          <strong>{page.title}</strong>
          {page.descrition}
        </span>
      </a>
    )}
</div>
