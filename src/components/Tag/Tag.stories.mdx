import { Meta, ArgsTable } from '@storybook/addon-docs/blocks';
import {Tag} from './Tag';


<Meta
  title="Components/Tag"
  argTypes={{
    closable: {
      description:'closable', 
      control: {
        type: 'boolean'
      },
      table: {
        defaultValue: {
          summary: 'false',
        }
      }
    },
    closeIcon: {
      description:'closeIcon', 
      control: {
        type: 'text'
      }
    },
    color: {
      description:'color', 
      control: {
        type: 'color'
      }
    },
    icon: {
      description:'icon', 
      control: {
        type: 'text',
      }
    },  
    visible: {
      description: 'visible',
      control: {
        type: 'boolean'
      },
      table: {
        defaultValue: {
          summary: 'true',
        }
      }
    },
    onClose: {
      description: 'onClose',
      control: {
        type: 'text'
      }
    },
    children: {
      description: 'children',
      control: {
        type: 'text'
      }
    }
  }}
  args={{
    visible: true,
    children: "Super tag !"
  }}
/>

### Tag

Use this playground to test the Tag component

<Canvas>
  <Story name="Standard">
    {args => {
      return <Tag {...args} />;
    }}
  </Story>
</Canvas>


<ArgsTable story="Standard" />